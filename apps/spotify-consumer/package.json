{
  "name": "@darwin-portfolio/spotify-consumer",
  "description": "Firebase Functions package, auto generated by @simondotm/nx-firebase",
  "scripts": {
    "lint": "cd ../../ && nx lint spotify-consumer",
    "build": "cd ../../ && nx build spotify-consumer",
    "serve": "yarn build && firebase emulators:start --only functions --config ../../firebase.spotify-consumer.json",
    "shell": "yarn build && firebase functions:shell",
    "start": "yarn shell",
    "deploy": "firebase deploy --only functions --config ../../firebase.spotify-consumer.json",
    "logs": "firebase functions:log"
  },
  "engines": {
    "node": "12"
  },
  "main": "src/main.tsx",
  "dependencies": {},
  "devDependencies": {},
  "private": true
}
