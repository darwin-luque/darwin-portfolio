{
  "name": "@darwin-portfolio/spotify-consumer",
  "scripts": {
    "lint": "cd ../../ && nx lint spotify-consumer",
    "build": "cd ../../ && nx build spotify-consumer",
    "serve": "yarn build && firebase emulators:start --only hosting --config ../../firebase.spotify-consumer.json",
    "shell": "yarn build && firebase hosting:shell",
    "start": "yarn shell",
    "deploy": "firebase deploy --only hosting --config ../../firebase.spotify-consumer.json",
    "logs": "firebase hosting:log"
  },
  "engines": {
    "node": "12"
  },
  "main": "src/main.tsx",
  "dependencies": {},
  "devDependencies": {},
  "private": true
}
